// EventData - ย้ายข้อมูลไปรวมใน usersData.js แล้ว
// ไฟล์นี้เก็บไว้เพื่อ backward compatibility
import { eventsData } from '../../../data/usersData';

const EventData = eventsData;

export default EventData;