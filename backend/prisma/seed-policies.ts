import { PrismaClient } from '@prisma/client';
import { Pool } from 'pg';
import { PrismaPg } from '@prisma/adapter-pg';
import 'dotenv/config';

const connectionString = process.env.DIRECT_URL || process.env.DATABASE_URL;
if (!connectionString) throw new Error('DATABASE_URL is not set');

const pool = new Pool({ connectionString });
const adapter = new PrismaPg(pool);
const prisma = new PrismaClient({ adapter });

async function seedPolicies() {
  console.log('📜 Seeding policies...');

  await prisma.policy.upsert({
    where: { key: 'privacy' },
    update: {},
    create: {
      key: 'privacy',
      title: 'นโยบายความเป็นส่วนตัว',
      content: `# นโยบายความเป็นส่วนตัว (Privacy Policy)

**วันที่มีผลบังคับใช้:** 1 มกราคม 2569

## 1. ข้อมูลที่เราเก็บรวบรวม
ระบบ EasyCheck เก็บรวบรวมข้อมูลเท่าที่จำเป็นสำหรับการลงเวลาทำงาน ได้แก่:
- ชื่อ-นามสกุล, รหัสพนักงาน, อีเมล, เบอร์โทรศัพท์
- ข้อมูลตำแหน่ง (GPS) ขณะลงเวลาเข้า-ออกงาน
- เวลาเข้า-ออกงาน, ประวัติการลา, ประวัติกิจกรรม

## 2. วัตถุประสงค์ในการใช้ข้อมูล
- บันทึกและตรวจสอบเวลาทำงานของพนักงาน
- จัดทำรายงานการเข้างานสำหรับฝ่ายบุคคล
- แจ้งเตือนและสื่อสารภายในองค์กร

## 3. การเปิดเผยข้อมูล
- ข้อมูลจะถูกเปิดเผยเฉพาะกับผู้บริหารและฝ่ายบุคคลที่มีสิทธิ์เข้าถึงเท่านั้น
- ไม่เปิดเผยข้อมูลส่วนบุคคลแก่บุคคลภายนอกโดยไม่ได้รับความยินยอม

## 4. การรักษาความปลอดภัยข้อมูล
- ข้อมูลถูกเข้ารหัสระหว่างการส่งผ่าน (SSL/TLS)
- รหัสผ่านถูกเข้ารหัสด้วย bcrypt
- มีระบบ Audit Log บันทึกการเข้าถึงข้อมูลทุกครั้ง

## 5. สิทธิ์ของเจ้าของข้อมูล
พนักงานมีสิทธิ์:
- เข้าถึงข้อมูลส่วนบุคคลของตนเอง
- ขอแก้ไขข้อมูลที่ไม่ถูกต้อง
- ร้องเรียนกรณีข้อมูลถูกนำไปใช้อย่างไม่เหมาะสม

## 6. การติดต่อ
หากมีข้อสงสัยเกี่ยวกับนโยบายนี้ กรุณาติดต่อฝ่ายบุคคลหรือผู้ดูแลระบบ`,
      version: '1.0',
    },
  });

  await prisma.policy.upsert({
    where: { key: 'terms' },
    update: {},
    create: {
      key: 'terms',
      title: 'ข้อกำหนดการใช้งาน',
      content: `# ข้อกำหนดการใช้งาน (Terms of Service)

**วันที่มีผลบังคับใช้:** 1 มกราคม 2569

## 1. การยอมรับข้อกำหนด
การใช้งานระบบ EasyCheck ถือว่าท่านยอมรับข้อกำหนดและเงื่อนไขทั้งหมดที่ระบุไว้ในเอกสารนี้

## 2. การใช้งานระบบ
- ใช้ระบบเพื่อวัตถุประสงค์ในการลงเวลาทำงานเท่านั้น
- ห้ามใช้ระบบในทางที่ผิดกฎหมายหรือขัดต่อนโยบายบริษัท
- ห้ามแชร์บัญชีผู้ใช้หรือรหัสผ่านกับผู้อื่น
- ห้ามลงเวลาแทนผู้อื่นหรือปลอมแปลงข้อมูลการเข้างาน

## 3. ความรับผิดชอบของผู้ใช้
- รักษาความลับของรหัสผ่าน
- แจ้งผู้ดูแลระบบทันทีหากพบการใช้งานที่ผิดปกติ
- ตรวจสอบข้อมูลการลงเวลาของตนเองให้ถูกต้อง

## 4. ข้อจำกัดความรับผิด
- ระบบอาจมีการปิดปรับปรุงชั่วคราวเพื่อบำรุงรักษา
- บริษัทไม่รับผิดชอบต่อความเสียหายจากการใช้งานที่ผิดวัตถุประสงค์

## 5. การเปลี่ยนแปลงข้อกำหนด
บริษัทขอสงวนสิทธิ์ในการเปลี่ยนแปลงข้อกำหนดโดยจะแจ้งให้ทราบล่วงหน้าผ่านระบบประกาศ`,
      version: '1.0',
    },
  });

  console.log('✅ Policies seeded successfully');
  await prisma.$disconnect();
  await pool.end();
}

seedPolicies().catch((e) => {
  console.error('❌ Seed failed:', e);
  process.exit(1);
});
